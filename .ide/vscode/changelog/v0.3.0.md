## v0.3.0

> **Note:** The standalone Vitest and Rust Test extensions can now be disabled
> or uninstalled — `CMK Tests` discovers and runs both directly via Bazel.

- **fix**: pin requirements.txt watchers to known paths
- **feat**: add C++ profile (clangd)
- **fix**: scope pre-commit progress to commits
- **fix**: rename `CMK>Bazel Tests` to `CMK Tests`
- **feat**: render CMK Tests to mirror repo file structure
- **feat**: enable vitest_test in bazel test controller (CMK-34651)
- **feat**: enable rust_test in bazel test controller (CMK-34652)
- **feat**: enable cc_test in bazel test controller (CMK-34653)
- **feat**: enable py_doc_test in bazel test controller (CMK-34654)
- **style**: switch to error icon on failed tests
- **perf**: switch to `find` for bazel test target discovery
- **revert**: remove `vitest.explorer` from recommended UI extensions
- **revert**: remove `swellaby.vscode-rust-test-adapter` from recommended Rust extensions
- **revert**: remove vitest related setting recommendations
- **revert**: remove vitest related commands
- **revert**: remove `cmk.ruff.testOnDemand` setting
- **revert**: remove `cmk.vitest.testOnDemand` setting
