## v0.4.0

> **Note:** The sidebar gains a top **Cockpit** that summarises the Issues view
> into 3-4 collapsible domain rows (Builds · Settings · Health · Git). The
> Getting Started wizard, the Builds list, the version-update banner, and the
> IDE Health "Apply All" header all move into the Cockpit so the rest of the
> sidebar shrinks. Density is now compact by default.

### Cockpit

- **feat**: add Cockpit overview with Builds / Settings / Health / Git rows
- **feat**: per-row severity badge, primary action, and per-item drill-down
- **feat**: family-tag pill (Python / UI / Markdown / …) on drill-down rows
- **feat**: Health row absorbs version-mismatch, required-extensions, devSite, and missing-config issues
- **feat**: move Getting Started onboarding wizard into the Cockpit (top of section)
- **feat**: per-item dismiss-X (jemalloc recommendation, pre-commit warning) with `Restore` action

### Severity model

- **feat**: `venv` missing → critical (breaks mypy / Pylance / ruff / format)
- **feat**: setting drift on an active profile family → critical (was warning)
- **feat**: setting drift on an inactive profile family now surfaces as warning (was filtered out)
- **feat**: required extension missing → critical when family is active or always-on, warning otherwise
- **feat**: Pylance crashed / extension inactive → critical (with startup grace window)
- **feat**: extension version mismatch → critical on minor/major, warning on patch
- **feat**: `info` severity tier for user-dismissed items (blue, no auto-expand, no row action)

### Activity feed (new section)

- **feat**: chronological feed of the last 50 CMK events
- **feat**: 200-entry ring buffer fed by every `log()` / `warn()` / `error()` call in `core/log.ts`
- **feat**: categories derived from message-prefix regex
- **feat**: auto-refresh on new events

### Issues view

- **feat**: sort order critical → warning → domain → label
- **feat**: severity-tinted icons and domain-prefixed labels
- **feat**: action verb inline in description (e.g. "needs building · Build")
- **feat**: "N critical · N warning" view-title summary
- **feat**: stable ids preserved across refreshes (no selection jumps)

### Profiles

- **feat**: profile cards reflect Cockpit severity for their builds + settings family
- **feat**: WARN / CRIT badges and colours on profile cards

### Git domain

- **feat**: pre-commit hook bypassed surfaces as warning
- **feat**: pre-commit hook never installed surfaces as info (`cmk.installPreCommit` runs `pre-commit install`)
- **feat**: `tests/qa-test-data` submodule dirty surfaces as warning
- **feat**: `cmk.fixQaTestDataSubmodule` — destructive reset to tracked gitlink

### Mypy targets

- **feat**: cockpit surfaces staged target changes as a warning chip ("N pending · Apply")
- **feat**: cockpit surfaces dismissed file-open prompts as info chips ("N dismissed · Review")
- **feat**: `cmk.mypy.reviewDismissedPrompts` — multi-select picker over the dismissed list
- **feat**: dismiss-X on the cockpit chip clears the dismissed list (`cmk.mypy.clearDismissedPrompts`)
- **feat**: dmypy daemon health — flag the daemon when `.vscode/.mypy.ini` has been edited since it started, action `cmk.mypy.restartDmypy`

### Bazel disk cache

- **feat**: cockpit warns when the Bazel disk cache (path read from `.bazelrc`) exceeds `cmk.bazel.cacheSizeWarnGiB` (default 50 GiB, user-scope setting)
- **feat**: `cmk.bazel.cleanDiskCache` — delete the disk cache after confirmation; the next build refills it

### Performance

- **perf**: configuration debounce 500 ms → 100 ms
- **perf**: post-task delay 1000 ms → 200 ms
- **perf**: lightweight `refreshOverview()` fast path
- **perf**: 5 s Cockpit auto-refresh interval
- **perf**: `.venv/bin/python` watcher
- **perf**: `currentBranch()` reads `.git/HEAD` via `fs` (no `git` subprocess)
- **perf**: `${which:…}` resolver walks `$PATH` via `fs.existsSync` with per-session cache

### Startup benchmarks

- **feat**: per-version background bands on the chart
- **feat**: per-version average lines with numeric labels
- **feat**: separator on VS Code version change in addition to extension version / branch
- **feat**: `cmk.benchmarkTrimRecent` — drop the last N benchmark runs from workspace history

### Density and visibility

- **feat**: compact density is the new default (set via `cmk.sidebar.density`)
- **revert**: remove in-cockpit density toggle
- **feat**: default visibility — Cockpit + OMD expanded; Environment, IDE Health, Issues, Activity collapsed; Profiles hidden

### Tests

- **test**: `domainSummary` + activity tests live in subfolders matching their source
