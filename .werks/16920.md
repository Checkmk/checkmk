[//]: # (werk v2)
# Aerohive HiveManager NG: migrate special agent to the ExtremeCloud IQ API

key        | value
---------- | ---
date       | 2026-06-12T07:07:23.532625+00:00
version    | 2.4.0p34
class      | fix
edition    | cre
component  | checks
level      | 2
compatible | no

The special agent for Aerohive HiveManager NG used the legacy Aerohive Developer
API (base URL `https://[rdc-id].aerohive.com/xapi/`, endpoint
`GET /xapi/v1/monitor/devices`).

Extreme Networks announced in field notice FN-2026-526 that the legacy Aerohive
Developer API will be permanently shut down on July 12, 2026. After that date all
requests to the legacy endpoints fail, so the special agent would stop delivering
data.

The special agent now talks to the ExtremeCloud IQ API (XAPI) instead:

* The device list is retrieved from `GET /devices` (base URL
  `https://api.extremecloudiq.com`) instead of the legacy
  `/xapi/v1/monitor/devices` endpoint.
* Authentication now uses the ExtremeCloud IQ username and password: the agent
  obtains a bearer token from `POST /login` and releases it again with
  `POST /logout`. The obsolete `ownerId` query parameter and the legacy
  `X-AH-API-CLIENT-ID`, `X-AH-API-CLIENT-SECRET` and
  `X-AH-API-CLIENT-REDIRECT-URI` headers are no longer sent.

As a consequence the setup rule "Aerohive HiveManager NG / ExtremeCloud IQ" has
been simplified. It now only asks for the API base URL, the username and the
password. The fields "Numerical ID of the VHM", "API access token", "Client ID",
"Client secret" and "Redirect URL" have been removed.

This change is incompatible: existing rules are migrated automatically and keep
the configured URL, but because the authentication scheme changed completely you
have to review them manually:

* Set the URL to the new ExtremeCloud IQ base URL `https://api.extremecloudiq.com`
  (the previously configured legacy URL no longer works).
* Enter the ExtremeCloud IQ username and password.

The produced service checks ("Client &lt;hostname&gt;") are unchanged.
