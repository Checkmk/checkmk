[//]: # (werk v2)
# Prevent alertstats view from initially freezing on large sites

key        | value
---------- | ---
date       | 2025-02-25T10:00:12+00:00
version    | 2.5.0b1
class      | fix
edition    | cre
component  | multisite
level      | 1
compatible | no

For large sites, the alertstats view freezes before you have a chance to filter the result.

This werk introduces new default settings for the Alert Statistics view:
Searching with the filter must now be done explicitly for the view ("Show data only on search" is enabled by default).
In addition, the default for the "logtime from" filter has been changed from 31 to 7 to keep the default search result smaller.

If you want to use the Alert Statistics view in a dashboard without having to apply the filter manually, you have to:

* clone the view,
* uncheck "Show data only on search",
* use the cloned view in the dashboard.

When using the alertstats view without explicitly setting "logtime from" in any dashboard, you need to set "logtime from" = 31 to keep the same behaviour as before.