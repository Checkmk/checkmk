[//]: # (werk v2)
# agent_ipmi_sensors: fix error reporting

key        | value
---------- | ---
date       | 2026-02-19T13:41:14+00:00
version    | 3.0.0b1
class      | fix
edition    | cre
component  | checks
level      | 1
compatible | yes


The error reporting mechanism of special agents only reports the last line of
stderr to the checkmk user interface.

For `agent_ipmi_sensors` the error provided by `ipmitool` or `freeipmi` was
simply handed over, but contained a trailing `\n`.

This lead to the situation, that the error `'.` was reported.

Now, before handing the errors over, the `\n` are replaced so the error fits in
a single line.
