[//]: # (werk v2)
# mk_oracle: fix unwanted output in oracle_asm_diskgroup section

key        | value
---------- | ---
date       | 2026-04-07T11:04:34+00:00
version    | 2.5.0b4
class      | fix
edition    | cre
component  | checks
level      | 1
compatible | yes

When using `mk_oracle` to check remote instances, it was possible that the
following output was visible in the agent output:

```
<<<oracle_asm_diskgroup>>>
cat: '[$MK_VARDIR]/tmp/mk_oracle_tmp_dir/tasks/[number]/SID*': No such file or directory
```

This has been fixed.
