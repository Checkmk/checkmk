[//]: # (werk v3)
# Changed discovery ruleset names in the REST API

key        | value
---------- | ---
date       | 2026-05-21T12:01:41.401639+00:00
version    | 2.6.0b1
class      | feature
edition    | community
component  | rest-api
level      | 1
compatible | no

This affects users who are configuring service discovery rulesets via Checkmk's REST API version 1.0.

When referring to discovery rulesets, their names now should be prefixed with `discovery_parameters:`.
This is consistent with the HW/SW inventory parameters (`inv_parameters:`) and the check parameters ( `checkgroup_parameters:`).

For example:
The ruleset _"File system discovery"_ used to be referred to by `inventory_df_rules` -- now it should be `discovery_parameters:inventory_df_rules`.

During the lifetime of Checkmk 3.0 both values will be accepted.
Adding the prefix becomes mandatory in Checkmk 3.1.
