[//]: # (werk v2)
# PDF export: Fix black boxes instead of thin spaces

key        | value
---------- | ---
date       | 2025-10-31T14:25:12+00:00
version    | 2.4.0p16
class      | fix
edition    | cre
component  | reporting
level      | 1
compatible | yes

Fix black boxes appearing as thousands separators when exporting 
a service view as PDF.

Thin spaces are now replaced with regular spaces when exporting 
a pdf, so that PDF viewers can render them without the need of
embedding a font in the document.
