[//]: # (werk v3)
# Fix status codes on ajax calls

key        | value
---------- | ---
date       | 2026-03-31T09:39:49.754612+00:00
version    | 2.5.0b3
class      | fix
edition    | community
component  | multisite
level      | 1
compatible | yes

Previously, when an AJAX request triggered an error (e.g. due to an unauthenticated request), the server
always responded with `200 OK` containing a JSON error body like:

    {"result_code": 1, "result": "Not authenticated: You are not authenticated.", "severity": "error"}

This caused AJAX error handlers that check HTTP status codes to not trigger, resulting in the raw JSON
being rendered in the UI instead of proper error handling.

The server now returns the correct HTTP status code (e.g. `401` for unauthenticated requests) alongside
the JSON error body.
