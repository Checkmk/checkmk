[//]: # (werk v2)
# fix: Pure storage agent not sending SNI

key        | value
---------- | ---
date       | 2026-02-17T15:16:34+00:00
version    | 3.0.0b1
class      | fix
edition    | cre
component  | checks
level      | 1
compatible | yes


The Pure Storage Flash Array special agent now properly implements Server Name 
Indication (SNI) during TLS handshakes when using the `--cert-server-name` option.

When connecting to Pure Storage devices by IP address while specifying
a hostname via `--cert-server-name`, the agent failed to properly communicate the 
expected hostname during the SNI portion of the TLS handshake. 

The agent now properly implements SNI by setting both the hostname 
validation and the server hostname for SNI negotiation, following urllib3 
best practices. This ensures the correct certificate is requested and validated 
during connection establishment.
