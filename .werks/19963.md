[//]: # (werk v3)
# Fix Event Console case-insensitive regex filter operator (~~)

key        | value
---------- | ---
date       | 2026-05-29T14:27:52.286023+00:00
version    | 2.5.0p6
class      | fix
edition    | community
component  | ec
level      | 1
compatible | yes

Previously, the _Message/Text of event (regex)_ filter on the Event Console
Events screen returned no results, even for patterns that should clearly match.
For example, filtering with `connection` would show nothing despite events with
text like `Connection Error` being present.

This has been fixed. The filter now correctly performs case-insensitive regex
matching against event messages.
