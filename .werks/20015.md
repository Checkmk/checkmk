[//]: # (werk v2)
# check_cert: Remove duplicated error details on connection failure

key        | value
---------- | ---
date       | 2026-06-26T09:41:57.598813+00:00
version    | 2.4.0p34
class      | fix
edition    | cre
component  | checks
level      | 1
compatible | yes

Previously, when the active check _Check certificates_ (check_cert) failed to
connect to the target host (for example on a TLS handshake error), the service
output was hard to read: the full multi-line error report was placed into the
service summary, and the "Caused by" error chain was shown twice in the service
details.

This has been fixed. The service summary now shows a single, concise reason for
the failure, while the complete error chain - including the "Caused by" block -
appears exactly once in the service details.

