[//]: # (werk v3)
# cmk CLI: show the error message instead of a traceback

key        | value
---------- | ---
date       | 2026-07-28T11:06:50.389543+00:00
version    | 2.5.0p11
class      | fix
edition    | community
component  | core
level      | 1
compatible | yes

General exceptions were re-raised unconditionally.
So the `cmk` CLI showed a user a Python traceback for every problem.

With this change the `--debug` option gets honored for all generic exceptions:

* Without it, print the exception (including its type) and exit.
* With it, re-raise and display the full traceback.

The crash report is still written in either case.
Its Crash-ID is now part of the message, so that a user is aware of it.

