[//]: # (werk v3)
# Fix crash on the schedule downtime form

key        | value
---------- | ---
date       | 2026-08-18T10:34:01.391813+00:00
version    | 3.0.0b1
class      | fix
edition    | community
component  | multisite
level      | 1
compatible | yes

Opening the "Schedule downtime" command form crashed with
`OverflowError: timestamp out of range for platform time_t` when one of
the configured downtime duration presets had a duration so long that its
end date could not be represented by the operating system.

Such a preset is now left out of the list of duration buttons, so the
form opens again and the remaining presets can be used. The presets are
configured under the global setting "Downtime duration presets".
