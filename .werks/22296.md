[//]: # (werk v3)
# Azure DB for MySQL and PostgreSQL: Replication service crashed

key        | value
---------- | ---
date       | 2026-09-16T12:19:04.755714+00:00
version    | 3.0.0b1
class      | fix
edition    | community
component  | checks
level      | 1
compatible | yes

The services _Azure/DB for MySQL Replication_ and _Azure/DB for PostgreSQL
Replication_ of the current Azure integration (`azure_v2`) crashed on every
check cycle with

```
AttributeError: 'Resource' object has no attribute 'get'
```

The check plugins expected a mapping of resources, but the section they
receive holds a single resource. The plugins now read the replication lag from
that resource directly.

After the update the services report the replication lag again. No
rediscovery is needed.
