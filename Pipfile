# ATTENTION: Most of this file is generated by omd/packages/python3-modules/python3-modules.make

[[source]]
url = "${PIPENV_PYPI_MIRROR}"
verify_ssl = true
name = "pypi"

[dev-packages]
astroid = "==2.13.5"  # used by testlib.pylint_checker_localization
azure-mgmt-resource = "==23.0.1"  # used by the release process to publish the azure image to the marketplace
azure-mgmt-compute = "==30.5.0"  # used by the release process to add a new image version
bandit = "==1.7.4"  # used by test/Makefile's test-bandit target
beautifulsoup4 = "==4.11.2"  # used by the GUI crawler and various tests
bson = "==0.5.10"  # used by test_mk_mongodb unit test
compiledb = "==0.10.1"  # used by the Livestatus/CMC Makefiles for building compile_command.json
devpi-client = "==6.0.2" # Managing the devpi server
docker = "==6.1.3"  # used by test_docker test and mk_docker agent plugin
dockerpty = "==0.4.1"  # used by dockerized tests for opening debug shells
freezegun = "==1.2.2"  # used by various unit tests
isort = "==5.12.0"  # used as a plugin for editors
lxml-stubs = "==0.4.0"  # used for type checking
mock = "==5.0.1"  # used in checktestlib in unit tests
pylint = "==2.15.5"  # used by test/Makefile's test-pylint target
hypothesis = "==6.68.0"    # used by unit tests
mypy = "==1.0.0"  # used by test/static/Makefile's test-mypy target
twine = "==4.0.2" # used to upload packages to internal PyPi mirror
types-six = "==1.16.21.4"  # used for type checking
types-requests = "==2.31.0.6"  # used for type checking
types-PyYAML = "==6.0.12.4"  # used for type checking
types-freezegun = "==1.1.10"  # used for type checking
types-markdown = "==3.5.0.20240106"  # used for type checking
types-mock = "==5.0.0.2"  # used for type checking
types-python-dateutil = "==2.8.19.6"  # used for type checking
types-simplejson = "==3.18.0.0"  # used for type checking
types-PyMySQL = "==1.0.19.2"  # used for type checking
types-pytz = "==2022.7.1.0"  # used for type checking
types-cryptography = "==3.3.23.2"  # used for type checking
types-redis = "==4.4.0.0"  # used for type checking
types-paramiko = "==2.12.0.1"
types-protobuf = "==4.21.0.5"  # used for type checking
types-setuptools = "==67.2.0.0"  # used for type checking
types-pyOpenSSL = "==23.0.0.0"  # used for type checking
types-waitress = "==2.1.4.4"  # used for type checking
playwright = "==1.30.0"  # used for in-browser testing
pydantic-factories = "==1.17.1"  # used for pydantic model testing;
pymongo = "==4.3.3"  # used by mk_mongodb agent plugin
pytest = "==7.2.1"  # used by various test/Makefile targets
pytest-cov = "==4.0.0"  # used (indirectly) by test/Makefile's test-unit-coverage-html target, see comment there
pytest-mock = "==3.10.0"  # used by quite a few unit/integration tests via the mocker fixture
preprocess = "==2.0.0" # implicit dependency of pytest-testmon, might be removed when testmon is updated
pytest-testmon = "==1.4.5"  # used for pre-commit checking via .pre-commit-config.yaml
pytest-profiling = "==1.7.0"  # used to analyze test run performance
pytest-random-order = "==1.1.0"  # used to test resiliency
pytest-repeat = "==0.9.1"  # used to test resiliency
pytest-xdist = "==3.2.0" # used to limit number of procs in e2e tests
responses = "==0.22.0"  # used for unit tests
webtest = "==3.0.0"  # used by WSGI based tests
pre-commit = "==3.0.4"  # used to fix / find issues before committing changes
flake8 = "==6.0.0"
autoflake = "==2.0.1"  # used to remove unused import statements
sphinx = "==5.3.0"  # used for the plugin API documentation, pinned for now, because 6.0.0 doesn't work well with other tools ("AttributeError: module 'docutils.nodes' has no attribute 'meta')
sphinx-autodoc-typehints = "==1.22"  # used for the plugin API documentation
sphinx-rtd-theme = "==0.5.1"  # used for the plugin API documentation
sphinxcontrib-plantuml = "==0.24.1"  # used for the API and architecture
sphinxcontrib-spelling = "==7.7.0"
3to2 = "==1.1.1"  # used for converting agent plugins from py3 to 2
fakeredis = {version = "==2.10.3", extras = ["lua"]}  # used for testing code which uses Redis (lua is needed for faking features like "lock")
python-lsp-server = "==1.7.1"  # Note: There are extras, but the default seems sensible.
python-lsp-black = "==1.2.1"  # black plugin for python-lsp-server
pyls-isort = "==0.2.2"  # isort plugin for python-lsp-server
pylsp-mypy = "==0.6.5"  # mypy plugin for python-lsp-server
mypy-protobuf = "==3.3.0" # for mypy
pipfile = "==0.0.2" # for generating import names during int tests
py-import-cycles = "==0.1.11"  # used in tests/Makefile
types-pysaml2 = "==1.0.1"  # typing extension for pysaml2
telnetlib3 = "==2.0.0"  # used by Windows Agent tests
ruff = "==0.0.240"  # newer versions have problems with the match statement
pexpect = "==4.8.0"  # used for update testing
httpx = "==0.26.0"  # for testing the agent-receiver

[packages]
setuptools_scm = "==4.1.2"  # needed by various setup.py
six = "==1.16.0"  # direct dependency + needed by bcrypt, cryptography, python-dateutil, vcrpy, python-active-directory
python-dateutil = "==2.8.2"  # direct dependency
PyYAML = "==6.0.1"  # needed by vcrpy
wrapt = "==1.14.1"  # needed by vcrpy
yarl = "==1.8.2"  # needed by vcrpy
multidict = "==6.0.4"  # needed by yarl
vcrpy = "==4.2.1"  # used by various unit tests to mock HTTP transactions
pycparser = "==2.21"  # needed by cffi
cffi = "==1.15.1"  # needed by cryptography, bcrypt
cryptography = "~=3.4"  # needed by paramiko. NOTE: rust is needed for version >=3.4, see https://cryptography.io/en/latest/changelog/#v3-4
bcrypt = "==3.2.2"  # needed by paramiko. NOTE: rust is needed for version >=4.0.0, see https://github.com/pyca/bcrypt#400
paramiko = "==3.4.0"
pyasn1 = "==0.4.8"  # needed by pysnmp
pyasn1-modules = "==0.2.8"  # needed by kubernetes
pycryptodomex = "==3.19.1"  # needed by pysnmp
ply = "==3.11"  # needed by pysmi, python-active-directory
pysmi-lextudio = "==1.1.13"  # needed by pysnmp
pysnmp-lextudio = "==5.0.29"  # needed by Event Console
snmpsim-lextudio = "==1.0.5"  # needed by pysnmp integration tests
chardet = "==5.1.0"  # needed by requests
urllib3 = "==1.26.18"  # needed by requests
pyOpenSSL = "==19.1.0"  # needed by requests with extras = ["security"]
pbr = "==5.11.0"  # needed by jira
pyghmi = "==1.5.13"  # needed by base for IPMI
requests = {version = "==2.31.0",extras = ["security"]}  # needed by DCD, connexion
pykerberos = "==1.2.1"  # needed by check_bi_aggr
requests-kerberos = "==0.12.0"  # needed by check_bi_aggr, versions >=0.13.0 depend on pyspnego, which pulls in gssapi, which in turn fails on SLES because of https://github.com/pythongssapi/python-gssapi/issues/212
isoduration = "==20.11.0"  # used by netapp ontap agent
Jinja2 = "==3.1.6"  # needed by Flask
more-itertools = "==9.0.0"  # needed by connexion through zipp, pytest
attrs = "==22.2.0"  # needed by jsonschema
importlib_metadata = "==5.2.0"  # needed by jsonschema
pyrsistent = "==0.18.0"  # needed by jsonschema
click = "==8.1.3"  # needed by Flask
werkzeug = "==3.0.6"  # direct dependency and needed by flask
jsonschema = "==4.17.3"  # needed by openapi-spec-validator
flask = "==3.0.2"  # direct dependency
pytz = "==2022.7"  # direct dependency
openapi-spec-validator = "==0.5.1"  # direct dependency
psutil = "==5.9.4"  # needed for omdlib
passlib = "==1.7.4"  # needed for omdlib
defusedxml = "==0.7.1"  # needed by jira
oauthlib = "==3.1.0"  # needed by requests-oauthlib and jira
requests-oauthlib = "==1.3.1"  # needed by jira
requests-toolbelt = "==0.10.1"  # needed by jira
docutils = "==0.19"  # needed by boto3 (aws)
jira = "==3.4.1"  # needed by jira
msal = "==1.26.0"  # needed by agent_azure
Pillow = ">10.1.0,<11.0"  # pinned to <11.0, because 11.0 changes types
python-ldap = "==3.4.3"  # needed by GUI (User sync), python-active-directory
dicttoxml = "==1.7.16"  # needed by GUI (API XML format)
Cython = "==3.0.11"  # needed by numpy & pymssql
numpy = "==1.24.2"  # needed by GUI (metrics)
reportlab = "==3.6.13"  # needed by GUI (reporting)
PyPDF3 = "==1.0.5"  # needed by GUI (reporting)
roman = "==3.3"  # needed by GUI (reporting)
cachetools = "==4.2.4"  # needed by kubernetes
google-auth = "==2.3.3"  # needed by kubernetes
websocket_client = "==1.4.2"  # needed by kubernetes
kubernetes = "==21.7.0"  # needed by kubernetes
jmespath = "==0.10.0"  # needed by boto3 (aws)
botocore = "==1.20.102"  # needed by boto3 (aws)
s3transfer = "==0.4.2"  # needed by boto3 (aws)
boto3 = "==1.17.102"  # needed by boto3 (aws)
python-snap7 = "==1.2"  # needed by Siemens PLC special agent
pymssql = "==2.3.1"  # needed by check_sql active check
PyMySQL = "==0.9.3"  # needed by check_sql active check
psycopg2-binary = "==2.9.5"  # needed by check_sql active check
apispec = "==5.2.2"  # direct dependency
marshmallow = "==3.19.0"  # direct dependency
marshmallow-oneofschema = "==3.0.1"  # direct dependency
mypy_extensions = "==0.4.3" # direct dependency
oracledb = "==2.5.1"  # used by active check_sql
apispec-oneofschema = "==3.0.0"  # direct dependency
typing_extensions = "==4.9.0"   # required by pydantic
pydantic = "==1.10.2"
paho-mqtt = "==1.6.1"  # needed for MQTT special agent
python-active-directory = "==2.0.1"
rrdtool = "==0.1.16"  # Actually we ship the binding which comes with rrdtool itself...
docstring_parser = "==0.15"  # direct dependency
pyprof2calltree = "==1.4.5"  # used for GUI/API profiling
repoze-profile = "==2.3"  # used for GUI/API profiling
pyparsing = "==3.0.9"  # direct dependency
ordered-set = "==4.1.0"  # needed by deepdiff
deepdiff = "==6.2.2"  # used for recording setup audit log
redis = "==4.6.0"  # needed by GUI (caching)
fido2 = "==0.9.3"  # used for GUI 2fa webauthn
tenacity = "==8.1.0"  # needed by opsgenie-sdk
opsgenie-sdk = "==2.1.5"  # needed by opsgenie_issues
protobuf = "==3.20.1"  # For development, Runtime modules are shipped by omd/packages/protobuf
semver = "==2.13.0"
uvicorn = "==0.20.0"  # used for running agent-receiver server, UPGRADES MIGHT BREAK agent-receiver/agent_receiver/worker.py
gunicorn= "==23.0.0" # used for running agent-receiver server
agent-receiver = {editable = true, path = "./agent-receiver"}
pysmb = "==1.2"  # used by SMB share special agent
google-cloud-monitoring = "==2.8.0"  # used by the gcp special agent
google-cloud-asset = "==3.8.0"  # used by the gcp special agent
exchangelib = "==5.4.0"  # used by active checks for mail
lxml = "==4.9.2"  # used via beautifulsoup4 as a parser and in the agent_netapp special agent
grpcio = "==1.53.2"  # might cause problems in Centos7 and SLES12
google-api-python-client = "==2.53.0"  # use rest api to query big query for gcp cost monitoring
setproctitle = "==1.3.2"  # used by mkbench/dcd/liveproxyd
black = {version = "==22.12.0", extras = ["d"]}  # used in our CI, the LSP-integration and in the OpenAPI part, "d" for broken black packaging only, see https://github.com/psf/black/issues/690
meraki = "==1.27.0"  # used by special agent Cisco Meraki
feedparser = "==6.0.10"  # used by special agent Microsoft Azure Status
pysaml2 = "==7.2.1"  # used by SSO via SAML2.0
azure-storage-blob = "==12.14.1"  # used for backups to Azure Blob Storage
azure-identity = "==1.12.0"  # used for backups to Azure Blob Storage
markdown = "==3.4.1"  # used for werk parsing
icalendar = "==5.0.7"  # used for importing .ical files
recurring_ical_events = "==2.0.2"  # used for parsing recurring events in icalendar objects
netapp-ontap = "==9.14.1.0"  # used by netapp special agent

[scripts]
werk = {call = "cmk.werks.cli:main()"}

[requires]
# As pipenv {sync,lock} does currently not support env variable expansion in and out Pipfile.lock,
# we do not require a python_version here and defines.make is our single point of truth.
# the PYTHON_VERSION will be injected during pipenv calls in Makefile.
