@echo off
rem File to run Integration Tests in the integration folder
rem should be called ONLY after successful build
rem checkmkservice wil be stopped if any

echo Integration tests are temporary disabled
exit /b 0
