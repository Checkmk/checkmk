#!/bin/bash
if [[ -z $1 ]]; then
    echo first argument must be the new default site
    exit
fi

echo "# This file is generated by $0" >/etc/apache2/sites-available/default_site.conf
echo "RedirectMatch ^/$ /$1/check_mk/" >>/etc/apache2/sites-available/default_site.conf
