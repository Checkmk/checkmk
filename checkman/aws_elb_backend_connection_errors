title: AWS ELB: Backend Errors
agents: aws
catalog: cloud/aws
license: GPLv2
distribution: check_mk
description:
 This check monitors the rate of connection errors between AWS/ELB
 and the registered instances.

 To make this check work you have to configure the related
 special agent {{Amazon Web Services (AWS)}}.

 No default levels for {WARN}/{CRIT} are set. These are
 configurable.

discovery:
 One service is created if all required metrics
 (BackendConnectionErrors) are given.
