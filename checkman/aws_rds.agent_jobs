title: AWS/RDS Agent Jobs
agents: aws
catalog: cloud/aws
license: GPL
distribution: check_mk
description:
 This check monitors the failed SQL server agent jobs.
 To make this check work you have to configure the related
 special agent {Amazon Web Services (AWS)}.

 The service goes {WARN} if at least one jobs fails.

item:
 Name of the instance and region name (to avoid duplicate services
 in case of multi-region monitoring).

inventory:
 One service per database will be created.
