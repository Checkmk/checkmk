title: Kubernetes: Replicas (set)
agents: kubernetes
catalog: containerization/kubernetes
license: GPLv2
distribution: check_mk
description:
 This check and the associated special agent (agent_kubernetes) are deprecated
 and will be removed in Checkmk version 2.2.0.

 Please configure the new special agent using the "Kubernetes" ruleset.

 This check monitors the number of replicas in a Kubernetes stateful set.
 The state of the check is dependent on the configuration of the stateful set
 in Kubernetes.

 The check returns {OK} if every replica is ready, {WARN} otherwise.

discovery:
 One service is created.
