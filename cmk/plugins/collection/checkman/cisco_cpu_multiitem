title: Cisco Devices: CPU Utilization (multiple Items)
agents: snmp
catalog: hw/network/cisco
license: GPLv2
distribution: check_mk

item:
 Index of CPU

description:
 Check CPU utilization of the last 5 minutes in percent on Cisco devices.

discovery:
 By default one service is created for each CPU.
 The discovery is configured via the discovery Ruleset "CPU multi item discovery".
 Checking only {"individual"} is equivalent to the default behaviour.
 If {"average"} is checked a {"average"} item will be discovered showing the
 average CPU of all individual CPUs.
