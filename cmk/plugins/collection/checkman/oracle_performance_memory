title: ORACLE tablespaces: perfomance data for memory
agents: linux, hpux, solaris, windows
catalog: app/oracle
license: GPLv2
distribution: check_mk
description:
 This subcheck is active if configured in the WATO rules 'Oracle performance discovery'.
 If this subcheck is active the SGA memory statistics of the oracle_performance main check are
 extracted into this check. Additionally this check also shows PGA memory statistics.

 For this subcheck it is possible to configure levels with the WATO rule 'Oracle Performance'.

item:
 The SID identifier.

discovery:
 One service per SID is created.
