title: ORACLE tablespaces: perfomance data for wait classes
agents: linux, hpux, solaris, windows
catalog: app/oracle
license: GPLv2
distribution: check_mk
description:
 This subcheck is active if configured in the WATO rules 'Oracle performance discovery'.
 If this subcheck is active the performance data of wait times for all existent wait classes is collected.

 For this subcheck it is possible to configure levels for these wait times with the WATO rule 'Oracle Performance'.

item:
 The SID identifier.

discovery:
 One service per SID is created.
