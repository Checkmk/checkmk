title: PrimeKey Appliance: VMs, RAID, EJBCA, Signserver, HSM status
agents: snmp
catalog: hw/primekey
license: GPLv2
distribution: check_mk
description:
 This check monitors the status [ok / not ok] of VMs, RAID, EJBCA, Signserver and HSM on the PrimeKey applicance.
 If the status is not ok, the service goes to CRIT.

item:
 One item for each of the following: VMs, RAID, EJBCA, Signserver, HSM.

discovery:
 One service is created for each item.
