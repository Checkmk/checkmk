#!/bin/bash
set -e

SITE=${SITE:-$(cat ../.site 2>/dev/null || true)}
SITE=${SITE:-$(omd sites --bare | head -n 1)}
ROOT=/omd/sites/$SITE

sudo cp -v *.py $ROOT/share/check_mk/modules/

if [ -z $ONLY_COPY ]; then
    sudo su - $SITE -c "cmk --debug -R"
fi
