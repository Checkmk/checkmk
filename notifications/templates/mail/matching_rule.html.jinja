<!DOCTYPE html>
<table width="100%"
       style="border-top: 1px solid #e5e5e5;
              border-collapse: separate;
              margin: 0 auto;
              max-width: 600px">
    <tr>
        <td>
            <p style="text-align: center; font-size: 12px;">
                This email was triggered by:
                (#{{ data.PARAMETER_MATCHING_RULE_NR }}) {{ data.PARAMETER_MATCHING_RULE_TEXT }}
            </p>
        </td>
    </tr>
</table>
