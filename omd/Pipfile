# This pipenv is *only* used for tools which are used during building an omd package
[dev-packages]
sphinx = "*"
sphinx-autodoc-typehints = "*"  # used for the plugin API documentation
sphinx-rtd-theme = "*"  # used for the plugin API documentation
checkmk-dev-tools = "~=0.1.60"  # provides tooling for build artifacts
python-dateutil = "~=2.9.0"  # most likely a bug in trickkiste: it's a transitive dependency which is not set

[requires]
python_version = "3.12"
