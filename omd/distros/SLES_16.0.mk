DISTRO_CODE     = sles16.0
OS_PACKAGES     =
OS_PACKAGES    += libcap-progs # needed for setting special file permissions
OS_PACKAGES    += cronie # needed for sites cron jobs
OS_PACKAGES    += net-tools # traceroute is needed for Checkmk parent scan
OS_PACKAGES    += traceroute # no longer provided by net-tools
OS_PACKAGES    += apache2
OS_PACKAGES    += apache2-utils # required for htpasswd2
OS_PACKAGES    += bind-utils # check_dns
OS_PACKAGES    += curl
OS_PACKAGES    += dialog
OS_PACKAGES    += libgd3
OS_PACKAGES    += graphviz
OS_PACKAGES    += libpng16-16
OS_PACKAGES    += libevent-2_1-7
OS_PACKAGES    += libltdl7
OS_PACKAGES    += libreadline8
OS_PACKAGES    += libuuid1
OS_PACKAGES    += logrotate
OS_PACKAGES    += pango
OS_PACKAGES    += php8-fastcgi
OS_PACKAGES    += php8-gd
OS_PACKAGES    += php8-iconv
OS_PACKAGES    += php8-mbstring
OS_PACKAGES    += php8-pear # not supported by its vendor
OS_PACKAGES    += php8-sockets
OS_PACKAGES    += php8-sqlite
OS_PACKAGES    += php8-openssl
OS_PACKAGES    += rsync
OS_PACKAGES    += samba-client
OS_PACKAGES    += rpcbind
OS_PACKAGES    += unzip
OS_PACKAGES    += xorg-x11-fonts-core
OS_PACKAGES    += freeradius-client-libs
OS_PACKAGES    += binutils # Needed by Checkmk Agent Bakery
OS_PACKAGES    += rpm-build # Needed by Checkmk Agent Bakery
OS_PACKAGES    += libgio-2_0-0 # needed by msitools/Agent Bakery
OS_PACKAGES    += cpio # needed for Agent bakery (solaris pkgs)
OS_PACKAGES    += poppler-tools # needed for preview of PDF in reporting
OS_PACKAGES    += libpcap1 # needed for ICMP of CMC
OS_PACKAGES    += libffi8 # needed for pyOpenSSL and dependant
OS_PACKAGES    += libjpeg62 # needed by PIL
OS_PACKAGES    += libgthread-2_0-0 # Needed by cmc (rrd library)
OS_PACKAGES    += libpq5
OS_PACKAGES    += libxslt1 # Needed for lxml etree processing
OS_PACKAGES    += which # no longer delivered with apache2
OS_PACKAGES    += bc # Needed by some third-party check plugins
OS_PACKAGES    += file # Needed by some third-party check plugins
USERADD_OPTIONS   = -M
ADD_USER_TO_GROUP = gpasswd -a %(user)s %(group)s
APACHE_CONF_DIR   = /etc/apache2/conf.d
APACHE_INIT_NAME  = apache2
APACHE_USER       = wwwrun
APACHE_CTL        = /usr/sbin/apachectl
APACHE_MODULE_DIR_64 = /usr/lib64/apache2-prefork
MOUNT_OPTIONS     =
INIT_CMD          = systemctl %(action)s %(name)s.service
