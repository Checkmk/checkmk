#!/bin/bash

# Alias: Enable automation helper
# Menu: Basic
# Description:
#  This option enables the automation helper, which
#  speeds up UI and Rest API interactions in most
#  cases.

case "$1" in
default)
    echo "on"
    ;;
choices)
    echo "on: enable"
    echo "off: disable"
    ;;
esac
