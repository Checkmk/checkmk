# Needed for bulk notifcations.
# Only execute cmk --notify when the microcore is currently not enabled.
* * * * * if [ ! -e ###ROOT###/etc/check_mk/conf.d/microcore.mk -a -d ###ROOT###/var/check_mk/notify/bulk ]; then cmk --notify send-bulks; fi
