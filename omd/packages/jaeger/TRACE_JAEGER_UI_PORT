#!/bin/bash

# Alias: Jaeger UI port
# Menu: Add-ons
# Description:
#  If receiving traces is configured, you can customize the UI port here.

case "$1" in
    choices)
        echo "@{NETWORK_PORT}"
        ;;
esac
