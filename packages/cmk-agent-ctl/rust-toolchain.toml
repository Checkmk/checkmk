[toolchain]
channel = "1.87.0" # must be in sync with install-rust-cargo.sh run.ps1

[toolchain.windows]
targets = ["i686-pc-windows-msvc"]

[toolchain.linux]
targets = ["x86_64-unknown-linux-musl", "x86_64-unknown-linux-gnu"]
