# --- Known environment variables defined in legacy config ---
# DBUSER ***
# --- Unified Config ---
---
oracle:
  main:
    connection:
      hostname: localhost
    authentication:
      username: ""
      password: ""
      type: wallet
    instances:
      - alias: WALLET_ALIAS
