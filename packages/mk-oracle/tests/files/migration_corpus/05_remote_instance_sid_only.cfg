# Remote-instance dimension: a REMOTE_INSTANCE_* with SID but an empty
# ninth TNS-alias field connects directly by SID. The migration must not
# synthesize an alias from the SID — the alias would win over the SID and
# force a tnsnames.ora lookup (CMK-37338).
DBUSER='sys:secret:SYSDBA:::'
REMOTE_INSTANCE_proddb='sys:secret:SYSDBA:10.201.4.215:1521::proddb:12.1:'
