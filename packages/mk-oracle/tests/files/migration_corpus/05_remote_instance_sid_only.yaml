# --- Known environment variables defined in legacy config ---
# DBUSER ***
# REMOTE_INSTANCE_proddb ***
# --- Unified Config ---
---
oracle:
  main:
    connection:
      hostname: localhost
    authentication:
      username: "sys"
      password: "secret"
      type: standard
      role: sysdba
    instances:
      - sid: PRODDB
        connection:
          hostname: 10.201.4.215
          port: 1521
        authentication:
          username: "sys"
          password: "secret"
          type: standard
          role: sysdba
