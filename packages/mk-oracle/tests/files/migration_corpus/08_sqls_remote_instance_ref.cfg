# SQLS dimension: SQLS_SIDS may reference a remote instance by the name of
# its REMOTE_INSTANCE_* variable. The metric must attach to that migrated
# instance instead of becoming a standalone SID starting with
# REMOTE_INSTANCE_ (CMK-37343).
DBUSER='user:pass'
REMOTE_INSTANCE_FOO_PROD='c##checkmk:secret:SYSDBA:dbhost07:1521::FOO_PROD:12.1:FOO_PROD'
SQLS_SECTIONS="mat_views"
mat_views () {
    SQLS_SIDS="REMOTE_INSTANCE_FOO_PROD"
    SQLS_SQL="mat_view_check.sql"
}
