# A drop-in file contributing a remote instance; its variables must be
# sourced like the main file's and migrate to an own instance entry
# (CMK-37378).
REMOTE_INSTANCE_DROPIN='drop_user:drop_pw:SYSDBA:drophost:1521::DROPIN:19.0:'
