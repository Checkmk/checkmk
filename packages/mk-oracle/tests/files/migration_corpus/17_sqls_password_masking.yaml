# WARNING: pdb_context: SQLS_SIDS 'PRODDB' is migrated next to SQLS_TNSALIAS 'PRODPDB', but the instance is resolved by its alias, so the SID no longer restricts the section
# WARNING: pdb_context: cannot read SQL file 'pdb_context_test.sql': No such file or directory (os error 2)
# --- Known environment variables defined in legacy config ---
# DBUSER ***
# SQLS.pdb_context.SQLS_DBPASSWORD ***
# SQLS.pdb_context.SQLS_DBUSER sys
# SQLS.pdb_context.SQLS_SIDS PRODDB
# SQLS.pdb_context.SQLS_SQL pdb_context_test.sql
# SQLS.pdb_context.SQLS_TNSALIAS PRODPDB
# SQLS_SECTIONS pdb_context
# WARNING: pdb_context: SQLS_SIDS 'PRODDB' is migrated next to SQLS_TNSALIAS 'PRODPDB', but the instance is resolved by its alias, so the SID no longer restricts the section
# WARNING: pdb_context: cannot read SQL file 'pdb_context_test.sql': No such file or directory (os error 2)
# --- Unified Config ---
---
oracle:
  main:
    connection:
      hostname: localhost
    authentication:
      username: "user"
      password: "pass"
      type: standard
    instances:
      - sid: PRODDB
        alias: PRODPDB
        custom_metrics:
          - pdb_context:
              path: pdb_context_test.sql
