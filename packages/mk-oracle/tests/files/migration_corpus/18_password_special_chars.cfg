# Formatting dimension: passwords with backslashes, double quotes and
# tabs must be escaped into valid, loadable YAML instead of producing a
# parse error (CMK-37598 class).
DBUSER='checkmk:pa\ss"w	d::localhost:1521:'
