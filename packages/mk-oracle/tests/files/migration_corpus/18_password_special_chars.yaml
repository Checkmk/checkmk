# --- Known environment variables defined in legacy config ---
# DBUSER ***
# --- Unified Config ---
---
oracle:
  main:
    connection:
      hostname: localhost
      port: 1521
    authentication:
      username: "checkmk"
      password: "pa\\ss\"w\td"
      type: standard
