# Formatting dimension: legacy section lists separated by tabs or
# multiple blanks must split into individual sections instead of one
# bogus section name (CMK-37606).
DBUSER='user:pass::localhost:1521:'
SYNC_SECTIONS='instance	sessions   undostat'
ASYNC_SECTIONS='tablespaces	rman'
