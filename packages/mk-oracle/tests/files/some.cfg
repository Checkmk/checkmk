# test file to check migration of agent bakery to new format
# use next sequence to test
# cargo run -- -M ./tests/files/some.cfg > /tmp/1.yml ; ./cargo_run run -- -c /tmp/1.yml
# Template for manual testing only: not referenced by any automated test.
# Created by Check_MK Agent Bakery.
# This file is managed via WATO, do not edit manually or you
# lose your changes next time when you update the agent.# Connection and authentication
DBUSER='system:$CI_ORA_TEST_PASSWORD::oracle-rocky-ci.lan.checkmk.net:1521:'
DBUSER_SID23=':::::'
ASMUSER='/::SYSASM:::' # Sections to run in foreground and wait for the result
SYNC_SECTIONS='instance performance processes sessions longactivesessions logswitches undostat recovery_area recovery_status dataguard_stats systemparameter' # Sections to run in the background, at a slower interval cached
ASYNC_SECTIONS='tablespaces rman jobs resumable locks' # Sections to run in foreground for ASM
SYNC_ASM_SECTIONS='instance processes' # Sections to run in the background for ASM
ASYNC_ASM_SECTIONS='asm_diskgroup' # Sections disabled for some selected SIDs
EXCLUDE_SID23='performance processes sessions longactivesessions logswitches undostat recovery_area recovery_status dataguard_stats systemparameter'
REMOTE_ORACLE_HOME='/u01/app/oracle/product/19.0.0/dbhome_1' # Path to the Oracle home directory on the remote host