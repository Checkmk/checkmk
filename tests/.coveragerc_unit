[run]
source =
    ../active_checks
    ../agents
    ../bin
    ../checkman
    ../checks
    ../cmk
    ../enterprise/cmk/cee
    ../tests/unit
    ../web
branch = True
# TEMPORARY HACK: We need to exclude all Python 3 stuff, not only this module...
omit =
    ../enterprise/cmk/cee/dcd/connectors/utils.py
