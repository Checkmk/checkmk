# BUILD file for @wnx_simpleini, see //bazel/rules:vendored_archive.bzl.
load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "simpleini",
    hdrs = [
        "ConvertUTF.h",
        "SimpleIni.h",
    ],
    includes = ["."],
    visibility = ["//visibility:public"],
)
