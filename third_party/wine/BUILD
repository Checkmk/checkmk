# Package for third_party/wine: lets BUILD.wine.bazel be referenced as
# @wine_linux_x86_64's build_file from MODULE.bazel, and brings create-archive
# under `bazel lint` (shellcheck) coverage.

sh_library(
    name = "create_archive",
    srcs = ["create-archive"],
)
